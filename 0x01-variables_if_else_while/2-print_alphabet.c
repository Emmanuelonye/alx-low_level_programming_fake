#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch1;

	for (ch1 = 'a'; ch1 <= 'z'; ch1++)
		putchar(ch1);
		putchar("\n");

	return (0);
}
